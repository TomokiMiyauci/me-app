query Blog($lang: String!) {
  allBlog: blogs(where: { language: { eq: $lang } }) {
    title
    description
    coverImage: coverImage
  }
}

query HomeByLang($lang: String!) {
  home: homes(where: { language: { eq: $lang } }) {
    ...HomeMeta
  }
}
