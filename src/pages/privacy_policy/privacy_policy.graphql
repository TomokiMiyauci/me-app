query PrivacyPolicy($lang: String!) {
  allLegalDocument: legal_documentConnection(
    filter: { type: { eq: "privacy_policy" }, language: { eq: $lang } }
  ) {
    edges {
      node {
        body
      }
    }
  }
}
