query BlogByLang($lang: String!) {
  blogs: blogs(where: { language: { eq: $lang } }) {
    ...PostsMeta
  }
}

query ArticlesByLang($lang: String!) {
  articles: posts(
    where: { language: { eq: $lang } }
    orderBy: {
      published_at: ASC
    }
  ) {
   ...Article
        slug
  }
}
